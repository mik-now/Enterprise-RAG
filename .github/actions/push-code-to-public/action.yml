name: "Push code to public main"

inputs:
  OPEA_PAT:
    description: 'OPEA repo token'
    required: true

runs:
  using: "composite"
  steps:
  - name: Fetch external repo
    uses: actions/checkout@v4
    with:
      repository: 'opea-project/Enterprise-RAG'
      ref: 'main'
      token: ${{ inputs.OPEA_PAT }}
      path: public_repo
      fetch-depth: 0

  - name: Fetch internal repo
    uses: actions/checkout@v4
    with:
      ref: 'main'
      path: internal_repo
      fetch-depth: 0

  - name: Merge code from internal main
    id: push
    run: |
      cd public_repo
      git config --global user.email "actions@github.com"
      git config --global user.name "Github Actions"
      git remote add opea_rag https://github.com/opea-project/Enterprise-RAG
      git remote add internal_rag ../internal_repo
      git fetch internal_rag
      LAST_COMMIT_SHA=$(git log internal_rag/main -1 --pretty=%h)
      git checkout internal_main || git checkout -b internal_main
      if [[ -z "$(git diff HEAD internal_rag/main --binary -- ':(exclude).github/')" ]]; then
        echo "No diff to patch. Finish successfully"
        exit 0
      fi
      git diff HEAD internal_rag/main --binary -- ":(exclude).github/" > update.patch
      git apply update.patch
      rm update.patch
      git reset -- .github
      if [ -d ".github" ]; then
        cd .github
        git clean -fd
        cd ..
      fi
      if [ -n "$(git status --porcelain)" ]; then
        echo "push=true" >> "$GITHUB_OUTPUT"
        git add --all
        git commit -m "Patch code up to ${LAST_COMMIT_SHA}" --signoff
      else
        echo "No changes to be commited";
      fi
    shell: bash

  - name: Verify git diff and push
    if: ${{ steps.push.outputs.push == 'true' }}
    run: |
      cd public_repo
      echo "git diff with .github/ excluded:"
      git diff internal_rag/main --name-only -- ":(exclude).github/*"
      if [[ -z "$(git diff internal_rag/main --name-only -- ":(exclude).github/*")" ]]; then
        echo "Diff check ok"
        git push --set-upstream opea_rag internal_main
      else
        echo "Incorrect diff, abort"
        exit 1
      fi
    shell: bash


  - name: Install sudo
    if: ${{ steps.push.outputs.push == 'true' }}
    run: apt update && apt -y install sudo
    shell: bash

  - name: Install Github CLI
    if: ${{ steps.push.outputs.push == 'true' }}
    run: |
      (type -p wget >/dev/null || (sudo apt update && sudo apt-get install wget -y)) \
      && sudo mkdir -p -m 755 /etc/apt/keyrings \
      && wget -qO- https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo tee /etc/apt/keyrings/githubcli-archive-keyring.gpg > /dev/null \
      && sudo chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg \
      && echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null \
      && sudo apt update \
      && sudo apt install gh -y
    shell: bash

  - name: Create PR
    if: ${{ steps.push.outputs.push == 'true' }}
    run: |
      cd public_repo
      gh pr create --base main --head internal_main --title 'Internal main' --body 'Internal main changes' || echo "PR already exists"
    shell: bash
    env:
      GH_TOKEN: ${{ inputs.OPEA_PAT }}
